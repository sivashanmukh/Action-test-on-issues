name: Test

on:
  issues:
    types: [opened, edited, milestoned, closed, reopened, assigned, unassigned]

jobs:
  request:
    runs-on: ubuntu-latest
    steps:
    - name: Github issues on Notion
      uses: rudderlabs/github-notion@0.0.0-beta


